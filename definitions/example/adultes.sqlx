config {
    type: "table",
    description: "Adultes users table",
    assertions: {
        //uniqueKey: ["first_name", "last_name", "email"],
        uniqueKeys: [
            ["id"],
            ["first_name", "last_name", "email"]
        ]
    }
}

SELECT
  *
FROM
  ${ref("users")}
WHERE
  DATE_DIFF(CURRENT_DATE(),DATE(birth_Date),YEAR)>=18
